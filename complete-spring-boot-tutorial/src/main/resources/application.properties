# Use wildcard "*" to expose all endpoints
#management.endpoints.web.exposure.include=*

#can also expose individual endpoints with comma-delimited list
management.endpoints.web.exposure.include=health,info
management.info.env.enabled=true

#Exclude individual endpoints with a comma-delimited list
#management.endpoints.web.exposure.exclude=health,info

info.app.name=My Super Cool App
info.app.description=A crazy fun app, yoohooo!!
info.app.version=1.0.0

#defining custom properties
developer.name=Manali Patil
developer.position=Software Engineer

#Change Spring Boot embedded server port
server.port=7070

#Set the context path of the application
#All requests should be prefixed /mycoolapp
server.servlet.context-path=/mycoolapp